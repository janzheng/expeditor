# Runtime artifacts
.expo/logs/
.expo/output/

# Registry (runtime state)
.sigbus/

# Test results (regeneratable by running tests)
tests/phase0/results/
tests/phase1-2/results/

# Deno cache
.deno/
.refine/

# Claude Code agent worktrees — runtime state for per-agent git
# worktrees, should never be tracked
.claude/worktrees/

# OS / editor junk — previously accidentally staged by refine's git add -A
.DS_Store
**/.DS_Store
# MCP auto-approve config — generated per-dev-machine, not committed
mcp-auto-approve.json
